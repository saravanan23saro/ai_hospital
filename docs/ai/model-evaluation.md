# Model evaluation

No metric is displayed until produced by an evaluation run and stored with dataset, feature, and model versions. Classification reports accuracy, precision, recall, F1, ROC-AUC, and calibration; duration regression reports MAE and RMSE; ranking reports Top-1, Top-3, and acceptance rates. Synthetic datasets and their metrics must be labeled `SYNTHETIC DEVELOPMENT DATA`.
