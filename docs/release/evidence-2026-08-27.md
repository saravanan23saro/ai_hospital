# Release evidence — 2026-08-27

## Automated engineering gates

Environment: local Docker Compose stack on Windows, Asia/Kolkata.

| Gate | Result | Evidence |
| --- | --- | --- |
| API unit and security tests | Pass | `mvn test` exited successfully. |
| Web production and TypeScript build | Pass | `npm.cmd run build`; Next.js generated all eight static routes, including `/`. |
| Advisory AI tests | Pass | `python -m pytest`; 2 tests passed. |
| Compose configuration | Pass | `docker compose config --quiet`. |
| Running-stack acceptance | Pass | API readiness, AI health, web portal, and deterministic ranking checks passed. |
| Browser journey acceptance | Pass | Playwright Chromium suite; homepage, patient registration/profile/sign-out, and administrator approval workspace; 3 tests passed. |
| Resilience and concurrent load | Pass | Normal AI path verified; advisory container stopped; API fallback header, model contract, and ordering verified; AI restored; 100 concurrent readiness requests completed in 242.7 ms. |
| Automated accessibility | Pass | Axe WCAG 2/2.1 A/AA scans passed on `/`, `/register`, `/patient`, `/staff`, and `/operations`. |
| Deployed security boundaries | Pass | Protected endpoints denied anonymous access; CSP, frame, MIME-sniffing, request-ID, and CORS allowlist checks passed. |
| PostgreSQL backup | Pass | `careflow-20260827-075738.dump`, SHA-256 `1F536009AC1B2CAC387C5B0D9C701ED1C30AE55AD1AEC8A22788E8C8BB9E6D2C`. |
| Isolated restore rehearsal | Pass | Restored into disposable `careflow_restore_check`; all 8 Flyway migrations found; verification database removed. |
| Monitoring | Pass | Four alerting rules loaded and healthy; `hospital-api` and `hospital-ai` scrape targets reported `up`. |

The backup is stored locally under `backups/`, which is excluded from source control because database archives may contain sensitive data.

## External release gates

The following remain pending and must be evidenced by accountable organization owners before any production or clinical-readiness claim:

- Clinical safety and model validation approval.
- Privacy/legal assessment, retention, consent, and processing agreements.
- Independent penetration test and accepted remediation.
- Production infrastructure, secrets, encryption, RBAC, audit retention, and alert routing verification.
- Production-equivalent disaster recovery rehearsal with measured RPO and RTO.
- Assistive-technology and representative-user accessibility review.
- Incident response, downtime, rollback, and clinical escalation exercises.

Artifact digest, approvers, production migration version, rollback owner, evidence links, and post-release review date must be added when the release candidate and deployment environment are selected.
